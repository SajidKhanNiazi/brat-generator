<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="bratBlur">
      <feGaussianBlur in="SourceGraphic" stdDeviation="2" result="blurred" />
    </filter>
  </defs>
  
  <!-- Lime green background -->
  <rect width="512" height="512" fill="#8ACE00" rx="80" />
  
  <!-- Blurry "b" letter — brat aesthetic -->
  <text
    x="256"
    y="360"
    font-family="Arial, Helvetica, sans-serif"
    font-weight="bold"
    font-size="360"
    fill="black"
    text-anchor="middle"
    filter="url(#bratBlur)"
  >b</text>
</svg>
